{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"liulab-data","text":"<p>Look up a GEO study, inspect its sequencing runs, and download the raw FASTQ \u2014 in a few lines of Python. <code>liulab-data</code> (import name <code>labdata</code>) talks to NCBI for you and keeps every object lazy, so nothing hits the network until you actually ask for something.</p> <p>New here? Jump to the GEO Series tutorial.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install liulab-data\n</code></pre> <p>Downloading FASTQ also needs two external tools, sra-tools and pigz. Install them from Bioconda/conda-forge:</p> <pre><code>conda install -c bioconda -c conda-forge sra-tools pigz\n</code></pre> <p>Working inside the lab's pixi project?</p> <p>Both tools are already in the project environment \u2014 <code>pixi install</code> gives you the Python package and <code>sra-tools</code>/<code>pigz</code> in one step.</p>"},{"location":"#configuration","title":"Configuration","text":"<p>NCBI asks every caller to identify themselves with a contact email. Set it once and you're ready:</p> <pre><code>export NCBI_EMAIL=\"you@lab.org\"     # required by NCBI\nexport NCBI_API_KEY=\"...\"           # optional \u2014 raises your rate limit\n</code></pre> <p>Tip</p> <p>Prefer not to use environment variables? Run <code>labdata ncbi configure</code> to save your email to <code>~/.cache/liulab-data/</code> once and forget about it.</p>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>GEO Series tutorial \u2014 from an accession to downloaded   FASTQ, step by step.</li> <li>API reference \u2014 every class and method in full detail.</li> </ul>"},{"location":"reference/","title":"API reference","text":"<p>liulab-data: data curation, download, and organization utilities for the Liu Lab.</p>"},{"location":"reference/#labdata.BioProject","title":"BioProject","text":"<pre><code>BioProject(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_Record</code></p> <p>An NCBI BioProject (<code>PRJNA000000</code>), resolved lazily through NCBI Entrez.</p> <p>BioProject is the umbrella record that groups a study's GEO Series and SRA data. It lives in the Entrez <code>bioproject</code> database, whose esummary uses the v2.0 <code>DocumentSummarySet</code> shape.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A BioProject accession, e.g. <code>\"PRJNA545296\"</code> (<code>^PRJ[A-Z]{2}\\d+$</code>, covering NCBI <code>PRJNA</code>, EBI <code>PRJEB</code>, and DDBJ <code>PRJDB</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed BioProject accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.BioProject.uid","title":"uid  <code>cached</code> <code>property</code>","text":"<pre><code>uid: str\n</code></pre> <p>The Entrez <code>bioproject</code> UID for this record.</p> <p>Raises:</p> Type Description <code>EntrezError</code> <p>If NCBI returns no matching <code>bioproject</code> record.</p>"},{"location":"reference/#labdata.BioProject.title","title":"title  <code>property</code>","text":"<pre><code>title: str\n</code></pre> <p>The project title.</p>"},{"location":"reference/#labdata.BioProject.name","title":"name  <code>property</code>","text":"<pre><code>name: str\n</code></pre> <p>The project name.</p>"},{"location":"reference/#labdata.BioProject.description","title":"description  <code>property</code>","text":"<pre><code>description: str\n</code></pre> <p>The free-text project description.</p>"},{"location":"reference/#labdata.BioProject.organism","title":"organism  <code>property</code>","text":"<pre><code>organism: str\n</code></pre> <p>The organism the project targets.</p>"},{"location":"reference/#labdata.BioProject.data_type","title":"data_type  <code>property</code>","text":"<pre><code>data_type: str\n</code></pre> <p>The project data type (e.g. <code>Transcriptome or Gene expression</code>).</p>"},{"location":"reference/#labdata.BioProject.registration_date","title":"registration_date  <code>property</code>","text":"<pre><code>registration_date: str\n</code></pre> <p>The project's registration date as reported by NCBI.</p>"},{"location":"reference/#labdata.BioProject.submitter","title":"submitter  <code>property</code>","text":"<pre><code>submitter: str\n</code></pre> <p>The submitting organization.</p>"},{"location":"reference/#labdata.BioProject.url","title":"url  <code>property</code>","text":"<pre><code>url: str\n</code></pre> <p>The NCBI BioProject web URL for this record.</p>"},{"location":"reference/#labdata.BioProject.series","title":"series  <code>cached</code> <code>property</code>","text":"<pre><code>series: list[Series]\n</code></pre> <p>The GEO Series (<code>GSE</code>) under this project, as :class:<code>Series</code> instances.</p> <p>One <code>elink</code> plus one batched <code>esummary</code>; each :class:<code>Series</code> is seeded with the UID and summary already fetched.</p>"},{"location":"reference/#labdata.BioProject.experiments","title":"experiments  <code>cached</code> <code>property</code>","text":"<pre><code>experiments: list[Experiment]\n</code></pre> <p>The SRA experiments (<code>SRX</code>) under this project, as :class:<code>Experiment</code> instances.</p>"},{"location":"reference/#labdata.Experiment","title":"Experiment","text":"<pre><code>Experiment(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_SraRecord</code></p> <p>An SRA experiment (<code>SRX000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>An SRA experiment accession, e.g. <code>\"SRX5921017\"</code> (<code>^[SED]RX\\d+$</code>, covering SRA/ENA/DDBJ).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed experiment accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.Experiment.title","title":"title  <code>property</code>","text":"<pre><code>title: str\n</code></pre> <p>The experiment title.</p>"},{"location":"reference/#labdata.Experiment.study","title":"study  <code>property</code>","text":"<pre><code>study: str\n</code></pre> <p>The parent study accession (<code>SRP\u2026</code>), or empty string if absent.</p>"},{"location":"reference/#labdata.Experiment.runs","title":"runs  <code>cached</code> <code>property</code>","text":"<pre><code>runs: list[Run]\n</code></pre> <p>The sequencing runs (<code>SRR</code>) of this experiment, as :class:<code>Run</code> instances.</p> <p>A run resolves to the same <code>sra</code> record as its experiment, so each :class:<code>Run</code> is seeded with this experiment's summary (no extra fetch).</p>"},{"location":"reference/#labdata.Platform","title":"Platform","text":"<pre><code>Platform(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_GdsRecord</code></p> <p>A GEO Platform (<code>GPL000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A GEO Platform accession, e.g. <code>\"GPL16791\"</code> (<code>^GPL\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed GEO Platform accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.Platform.sample_count","title":"sample_count  <code>property</code>","text":"<pre><code>sample_count: int | None\n</code></pre> <p>The number of samples GEO has on this platform, or <code>None</code> if unknown.</p>"},{"location":"reference/#labdata.Run","title":"Run","text":"<pre><code>Run(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_SraRecord</code></p> <p>An SRA run (<code>SRR000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>An SRA run accession, e.g. <code>\"SRR9000001\"</code> (<code>^[SED]RR\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed run accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.Run.total_spots","title":"total_spots  <code>property</code>","text":"<pre><code>total_spots: int | None\n</code></pre> <p>The number of spots (reads) in this run, or <code>None</code> if unreported.</p>"},{"location":"reference/#labdata.Run.total_bases","title":"total_bases  <code>property</code>","text":"<pre><code>total_bases: int | None\n</code></pre> <p>The number of bases in this run, or <code>None</code> if unreported.</p>"},{"location":"reference/#labdata.Run.is_public","title":"is_public  <code>property</code>","text":"<pre><code>is_public: bool\n</code></pre> <p>Whether the run's data is publicly accessible.</p>"},{"location":"reference/#labdata.Run.read_lengths","title":"read_lengths  <code>property</code>","text":"<pre><code>read_lengths: list[int]\n</code></pre> <p>The average length of each read (mate) sequenced per spot, in read order.</p> <p>Reads SRA's per-run <code>&lt;Statistics&gt;</code> from the full record (one EFetch, cached). A paired run yields two lengths, e.g. <code>[28, 94]</code>; a single-end run yields one. Empty when SRA reports no read statistics.</p>"},{"location":"reference/#labdata.Run.n_reads","title":"n_reads  <code>property</code>","text":"<pre><code>n_reads: int | None\n</code></pre> <p>The number of reads (mates) sequenced per spot, or <code>None</code> if unreported.</p>"},{"location":"reference/#labdata.Run.read_structure","title":"read_structure  <code>property</code>","text":"<pre><code>read_structure: str\n</code></pre> <p>The run's read structure as <code>{L1}+{L2}+\u2026</code> (e.g. <code>\"28+94\"</code>), or <code>\"\"</code>.</p> <p>A compact rendering of :attr:<code>read_lengths</code>: each read's rounded average length joined with <code>+</code> in read order. Empty when the lengths are unknown.</p>"},{"location":"reference/#labdata.Run.experiment","title":"experiment  <code>cached</code> <code>property</code>","text":"<pre><code>experiment: Experiment\n</code></pre> <p>The parent :class:<code>Experiment</code> (<code>SRX</code>) of this run.</p> <p>Shares this run's <code>sra</code> record, so the experiment is seeded from the same summary (no extra fetch).</p>"},{"location":"reference/#labdata.Run.files","title":"files  <code>cached</code> <code>property</code>","text":"<pre><code>files: list[RemoteFile]\n</code></pre> <p>Every file the SRA Data Locator lists for this run (one SDL request, cached).</p> <p>Includes both the normalized SRA archive file(s) and any original/submitted files (e.g. a 10X Genomics <code>possorted_genome_bam</code>). This is the data behind the Run Browser's \"Data access\" panel.</p> <p>Returns:</p> Type Description <code>list of RemoteFile</code> <p>All files SDL exposes for this run, each with its download locations.</p> <p>Raises:</p> Type Description <code>SdlError</code> <p>If the SDL request fails or the run is unknown/non-public.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; [(f.name, f.type) for f in Run(\"SRR20172067\").files]\n[('possorted_genome_bam_TC2_d15_1.bam', 'TenX'), ('SRR20172067', 'sra')]\n</code></pre>"},{"location":"reference/#labdata.Run.original_files","title":"original_files  <code>property</code>","text":"<pre><code>original_files: list[RemoteFile]\n</code></pre> <p>The original/submitted files for this run (the \"Original format\" listing).</p> <p>The submitter's own uploads \u2014 e.g. the 10X Genomics BAM behind a run whose <code>.sra</code> only stored a single read \u2014 as opposed to the normalized SRA archive. Filters :attr:<code>files</code> to the non-archive types.</p> <p>Returns:</p> Type Description <code>list of RemoteFile</code> <p>Files whose type is not a normalized SRA archive type; empty when the submission has no original-format files.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; [f.name for f in Run(\"SRR20172067\").original_files]\n['possorted_genome_bam_TC2_d15_1.bam']\n</code></pre>"},{"location":"reference/#labdata.Sample","title":"Sample","text":"<pre><code>Sample(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_GdsRecord</code></p> <p>A GEO Sample (<code>GSM000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A GEO Sample accession, e.g. <code>\"GSM3827114\"</code> (<code>^GSM\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed GEO Sample accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.Sample.series","title":"series  <code>property</code>","text":"<pre><code>series: Series | None\n</code></pre> <p>The parent :class:<code>Series</code>, or <code>None</code> if the summary lists none.</p>"},{"location":"reference/#labdata.Sample.platform","title":"platform  <code>property</code>","text":"<pre><code>platform: Platform | None\n</code></pre> <p>The :class:<code>Platform</code> this sample was run on, or <code>None</code>.</p>"},{"location":"reference/#labdata.Sample.experiments","title":"experiments  <code>cached</code> <code>property</code>","text":"<pre><code>experiments: list[Experiment]\n</code></pre> <p>The SRA experiments (<code>SRX</code>) for this sample, as :class:<code>Experiment</code> instances.</p>"},{"location":"reference/#labdata.Series","title":"Series","text":"<pre><code>Series(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_GdsRecord</code></p> <p>A GEO Series (<code>GSE000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A GEO Series accession, e.g. <code>\"GSE131907\"</code> (<code>^GSE\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed GEO Series accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.Series.summary","title":"summary  <code>property</code>","text":"<pre><code>summary: str\n</code></pre> <p>The Series summary/abstract text.</p>"},{"location":"reference/#labdata.Series.pubmed_id","title":"pubmed_id  <code>cached</code> <code>property</code>","text":"<pre><code>pubmed_id: str | None\n</code></pre> <p>The PubMed ID of the associated publication, or <code>None</code>.</p>"},{"location":"reference/#labdata.Series.samples","title":"samples  <code>cached</code> <code>property</code>","text":"<pre><code>samples: list[Sample]\n</code></pre> <p>The samples (<code>GSM</code>) belonging to this Series, as :class:<code>Sample</code> instances.</p>"},{"location":"reference/#labdata.Series.platforms","title":"platforms  <code>cached</code> <code>property</code>","text":"<pre><code>platforms: list[Platform]\n</code></pre> <p>The platforms (<code>GPL</code>) this Series uses, as :class:<code>Platform</code> instances.</p>"},{"location":"reference/#labdata.Series.experiments","title":"experiments  <code>cached</code> <code>property</code>","text":"<pre><code>experiments: list[Experiment]\n</code></pre> <p>The SRA experiments (<code>SRX</code>) linked to this Series, as :class:<code>Experiment</code> instances.</p>"},{"location":"reference/#labdata.Series.bioprojects","title":"bioprojects  <code>cached</code> <code>property</code>","text":"<pre><code>bioprojects: list[BioProject]\n</code></pre> <p>The BioProjects (<code>PRJNA\u2026</code>) linked to this Series, as :class:<code>BioProject</code> instances.</p> <p>One <code>elink</code> plus one batched <code>esummary</code>; each :class:<code>BioProject</code> is seeded with the UID and summary already fetched.</p>"},{"location":"reference/#labdata.Series.make_sra_run_table","title":"make_sra_run_table","text":"<pre><code>make_sra_run_table() -&gt; pandas.DataFrame\n</code></pre> <p>Build a tidy, run-level (<code>SRR</code>) table for this Series, like SRA Run Selector.</p> <p>Resolves the Series' SRA experiments and fetches NCBI's <code>runinfo</code> report in one batched EFetch per chunk, returning one row per sequencing run. The columns are SRA's <code>runinfo</code> fields (<code>Run</code>, <code>BioSample</code>, <code>Experiment</code>, <code>LibraryName</code>, <code>Platform</code>, \u2026) with a few renamed to the Run-Selector names (<code>AvgSpotLen</code>, <code>Bases</code>, <code>Instrument</code>). The reported size is <code>size_MB</code> \u2014 E-utilities <code>runinfo</code> does not expose an exact byte count.</p> <p>A <code>ReadStructure</code> column (<code>{L1}+{L2}+\u2026</code>, e.g. <code>\"28+94\"</code>) is added from each run's per-read <code>&lt;Statistics&gt;</code>, read from the full SRA XML with a second batched EFetch per chunk (see :attr:<code>Run.read_structure</code>); it is empty for runs SRA reports no read statistics for.</p> <p>Returns:</p> Type Description <code>DataFrame</code> <p>One row per run, key columns ordered first. Empty when the Series has no linked SRA runs.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; table = Series(\"GSE229022\").make_sra_run_table()\n&gt;&gt;&gt; table[[\"Run\", \"BioSample\", \"Instrument\"]].head()\n</code></pre> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def make_sra_run_table(self) -&gt; pandas.DataFrame:\n    \"\"\"Build a tidy, run-level (``SRR``) table for this Series, like SRA Run Selector.\n\n    Resolves the Series' SRA experiments and fetches NCBI's ``runinfo`` report\n    in one batched EFetch per chunk, returning one row per sequencing run. The\n    columns are SRA's ``runinfo`` fields (``Run``, ``BioSample``, ``Experiment``,\n    ``LibraryName``, ``Platform``, \u2026) with a few renamed to the Run-Selector\n    names (``AvgSpotLen``, ``Bases``, ``Instrument``). The reported size is\n    ``size_MB`` \u2014 E-utilities ``runinfo`` does not expose an exact byte count.\n\n    A ``ReadStructure`` column (``{L1}+{L2}+\u2026``, e.g. ``\"28+94\"``) is added\n    from each run's per-read ``&lt;Statistics&gt;``, read from the full SRA XML with\n    a second batched EFetch per chunk (see :attr:`Run.read_structure`); it is\n    empty for runs SRA reports no read statistics for.\n\n    Returns\n    -------\n    pandas.DataFrame\n        One row per run, key columns ordered first. Empty when the Series has\n        no linked SRA runs.\n\n    Examples\n    --------\n    &gt;&gt;&gt; table = Series(\"GSE229022\").make_sra_run_table()  # doctest: +SKIP\n    &gt;&gt;&gt; table[[\"Run\", \"BioSample\", \"Instrument\"]].head()  # doctest: +SKIP\n    \"\"\"\n    import pandas\n\n    sra_uids = self.client.elink(dbfrom=\"gds\", db=\"sra\", uid=self.uid)\n    frames: list[pandas.DataFrame] = []\n    structures: dict[str, str] = {}\n    for chunk in _chunked(sra_uids, _RUNINFO_CHUNK):\n        text = self.client.efetch(db=\"sra\", ids=chunk, rettype=\"runinfo\", retmode=\"text\")\n        if text.strip():\n            frames.append(pandas.read_csv(io.StringIO(text)))\n        xml = self.client.efetch(db=\"sra\", ids=chunk, rettype=\"full\", retmode=\"xml\")\n        for accession, block in _run_blocks(xml).items():\n            structures[accession] = _read_structure(_read_lengths(block))\n    if not frames:\n        return pandas.DataFrame()\n    table = pandas.concat(frames, ignore_index=True).rename(columns=_RUNINFO_RENAME)\n    table[\"ReadStructure\"] = [structures.get(run, \"\") for run in table[\"Run\"]]\n    leading = [column for column in _RUN_TABLE_COLUMNS if column in table.columns]\n    remaining = [column for column in table.columns if column not in leading]\n    return table.reindex(columns=leading + remaining)\n</code></pre>"},{"location":"reference/#labdata.Series.download","title":"download","text":"<pre><code>download(\n    output_dir: str | Path = \".\",\n    n_parallel: int = 1,\n    *,\n    max_size: str | None = None,\n    retries: int | None = None,\n    backoff: float | None = None,\n    keep_sra: bool = False,\n    verbose: bool = True,\n) -&gt; dict[str, bool]\n</code></pre> <p>Download FASTQ for every run of every SRA experiment in this Series.</p> <p>Lays the data out as <code>&lt;output_dir&gt;/&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;*.fastq.gz</code> \u2014 one directory per experiment under a directory named for this Series \u2014 via sra-tools (<code>prefetch</code> + <code>fasterq-dump</code>). Runs already marked done (a <code>.&lt;SRR&gt;.success</code> flag) are skipped; the whole Series is downloaded in parallel at the run (<code>SRR</code>) level. The resumable <code>prefetch</code> step is retried on failure, so this is safe to rerun on an unstable connection.</p> <p>Parameters:</p> Name Type Description Default <code>output_dir</code> <code>str or Path</code> <p>Parent directory; a <code>&lt;output_dir&gt;/&lt;GSE&gt;</code> subtree is created for this Series.</p> <code>\".\"</code> <code>n_parallel</code> <code>int</code> <p>Maximum runs to download concurrently across the whole Series. On a flaky link, fewer concurrent connections (<code>1</code> or <code>2</code>) is often more reliable.</p> <code>1</code> <code>max_size</code> <code>str</code> <p>Passed to <code>prefetch --max-size</code> (defaults to :data:<code>~labdata.geo.sratools.DEFAULT_MAX_SIZE</code>); raise it for runs that exceed <code>prefetch</code>'s 20G default.</p> <code>None</code> <code>retries</code> <code>int</code> <p>Attempts for the resumable <code>prefetch</code> network step per run (defaults to :data:<code>~labdata.geo.sratools.DEFAULT_RETRIES</code>).</p> <code>None</code> <code>backoff</code> <code>float</code> <p>Base seconds for the linear backoff between <code>prefetch</code> retries (defaults to :data:<code>~labdata.geo.sratools.DEFAULT_BACKOFF</code>).</p> <code>None</code> <code>keep_sra</code> <code>bool</code> <p>Keep each run's downloaded <code>.sra</code> next to its FASTQ (as <code>&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;.sra</code>) instead of deleting it after extraction.</p> <code>False</code> <code>verbose</code> <code>bool</code> <p>Print a download plan up front and one line per finished run to <code>stderr</code>. Set <code>False</code> to download silently.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>Maps each run accession to whether its data is present on completion.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Series(\"GSE229022\").download(\"./fastq\", n_parallel=4)\n{'SRR24084454': True, 'SRR24084455': True, ...}\n</code></pre> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def download(\n    self,\n    output_dir: str | Path = \".\",\n    n_parallel: int = 1,\n    *,\n    max_size: str | None = None,\n    retries: int | None = None,\n    backoff: float | None = None,\n    keep_sra: bool = False,\n    verbose: bool = True,\n) -&gt; dict[str, bool]:\n    \"\"\"Download FASTQ for every run of every SRA experiment in this Series.\n\n    Lays the data out as ``&lt;output_dir&gt;/&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;*.fastq.gz`` \u2014 one\n    directory per experiment under a directory named for this Series \u2014 via\n    sra-tools (``prefetch`` + ``fasterq-dump``). Runs already marked done (a\n    ``.&lt;SRR&gt;.success`` flag) are skipped; the whole Series is downloaded in\n    parallel at the run (``SRR``) level. The resumable ``prefetch`` step is\n    retried on failure, so this is safe to rerun on an unstable connection.\n\n    Parameters\n    ----------\n    output_dir : str or Path, default \".\"\n        Parent directory; a ``&lt;output_dir&gt;/&lt;GSE&gt;`` subtree is created for this Series.\n    n_parallel : int, default 1\n        Maximum runs to download concurrently across the whole Series. On a\n        flaky link, fewer concurrent connections (``1`` or ``2``) is often\n        more reliable.\n    max_size : str, optional\n        Passed to ``prefetch --max-size`` (defaults to\n        :data:`~labdata.geo.sratools.DEFAULT_MAX_SIZE`); raise it for runs that\n        exceed ``prefetch``'s 20G default.\n    retries : int, optional\n        Attempts for the resumable ``prefetch`` network step per run (defaults\n        to :data:`~labdata.geo.sratools.DEFAULT_RETRIES`).\n    backoff : float, optional\n        Base seconds for the linear backoff between ``prefetch`` retries\n        (defaults to :data:`~labdata.geo.sratools.DEFAULT_BACKOFF`).\n    keep_sra : bool, default False\n        Keep each run's downloaded ``.sra`` next to its FASTQ (as\n        ``&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;.sra``) instead of deleting it after extraction.\n    verbose : bool, default True\n        Print a download plan up front and one line per finished run to\n        ``stderr``. Set ``False`` to download silently.\n\n    Returns\n    -------\n    dict[str, bool]\n        Maps each run accession to whether its data is present on completion.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Series(\"GSE229022\").download(\"./fastq\", n_parallel=4)  # doctest: +SKIP\n    {'SRR24084454': True, 'SRR24084455': True, ...}\n    \"\"\"\n    from labdata.geo import sratools\n\n    kwargs = {\n        \"max_size\": max_size,\n        \"retries\": retries,\n        \"backoff\": backoff,\n    }\n    overrides = {key: value for key, value in kwargs.items() if value is not None}\n\n    base = Path(output_dir) / self.accession\n    tasks: list[tuple[Run, Path]] = []\n    for experiment in self.experiments:\n        srx_dir = base / experiment.accession\n        srx_dir.mkdir(parents=True, exist_ok=True)\n        tasks.extend((run, srx_dir) for run in experiment.runs)\n    return sratools._run_plan(\n        self.accession,\n        tasks,\n        n_parallel,\n        output_root=base,\n        verbose=verbose,\n        keep_sra=keep_sra,\n        **overrides,\n    )\n</code></pre>"},{"location":"reference/#labdata.SraDownloader","title":"SraDownloader","text":"<pre><code>SraDownloader(\n    experiment: Experiment, output_dir: str | Path = \".\"\n)\n</code></pre> <p>Download FASTQ for every run of one SRA experiment (<code>SRX</code>) with sra-tools.</p> <p>Parameters:</p> Name Type Description Default <code>experiment</code> <code>Experiment</code> <p>The SRA experiment whose runs (<code>SRR</code>) will be downloaded.</p> required <code>output_dir</code> <code>str or Path</code> <p>Parent directory. A subdirectory named by the experiment accession (<code>&lt;output_dir&gt;/&lt;SRX&gt;</code>) is created and populated with one gzipped FASTQ set per run, plus a <code>.&lt;SRR&gt;.success</code> marker per finished run.</p> <code>\".\"</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from labdata import Experiment\n&gt;&gt;&gt; SraDownloader(Experiment(\"SRX5921017\"), \"./fastq\").download(n_parallel=2)\n{'SRR9000001': True, 'SRR9000002': True}\n</code></pre> Source code in <code>src/labdata/geo/sratools.py</code> <pre><code>def __init__(self, experiment: Experiment, output_dir: str | Path = \".\") -&gt; None:\n    self.experiment = experiment\n    self.output_dir = Path(output_dir)\n</code></pre>"},{"location":"reference/#labdata.SraDownloader.srx_dir","title":"srx_dir  <code>property</code>","text":"<pre><code>srx_dir: Path\n</code></pre> <p>The destination directory for this experiment (<code>&lt;output_dir&gt;/&lt;SRX&gt;</code>).</p>"},{"location":"reference/#labdata.SraDownloader.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return an unambiguous representation.</p> Source code in <code>src/labdata/geo/sratools.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Return an unambiguous representation.\"\"\"\n    return f\"{type(self).__name__}({self.experiment.accession!r}, {str(self.output_dir)!r})\"\n</code></pre>"},{"location":"reference/#labdata.SraDownloader.download","title":"download","text":"<pre><code>download(\n    n_parallel: int = 1,\n    *,\n    max_size: str = DEFAULT_MAX_SIZE,\n    retries: int = DEFAULT_RETRIES,\n    backoff: float = DEFAULT_BACKOFF,\n    keep_sra: bool = False,\n    verbose: bool = True,\n) -&gt; dict[str, bool]\n</code></pre> <p>Download every run of the experiment, parallel at the run level.</p> <p>Creates :attr:<code>srx_dir</code>, then downloads each run's gzipped FASTQ into it. Runs with an existing <code>.&lt;SRR&gt;.success</code> flag are skipped; a run that fails is recorded as <code>False</code> without aborting the others.</p> <p>Parameters:</p> Name Type Description Default <code>n_parallel</code> <code>int</code> <p>Maximum runs to download concurrently. On a flaky link, fewer concurrent connections (<code>1</code> or <code>2</code>) is often more reliable.</p> <code>1</code> <code>max_size</code> <code>str</code> <p>Passed to <code>prefetch --max-size</code>; raise it for large runs (<code>prefetch</code> otherwise refuses anything over its 20G default).</p> <code>:data:`DEFAULT_MAX_SIZE`</code> <code>retries</code> <code>int</code> <p>Attempts for the resumable <code>prefetch</code> network step per run; a retry continues the partial download rather than restarting.</p> <code>:data:`DEFAULT_RETRIES`</code> <code>backoff</code> <code>float</code> <p>Base seconds for the linear backoff between <code>prefetch</code> retries.</p> <code>:data:`DEFAULT_BACKOFF`</code> <code>keep_sra</code> <code>bool</code> <p>Keep each run's downloaded <code>.sra</code> next to its FASTQ (as <code>&lt;SRX&gt;/&lt;SRR&gt;.sra</code>) instead of deleting it after extraction.</p> <code>False</code> <code>verbose</code> <code>bool</code> <p>Print a download plan up front and one line per finished run to <code>stderr</code>. Set <code>False</code> to download silently.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>Maps each run accession to whether its data is present on completion.</p> Source code in <code>src/labdata/geo/sratools.py</code> <pre><code>def download(\n    self,\n    n_parallel: int = 1,\n    *,\n    max_size: str = DEFAULT_MAX_SIZE,\n    retries: int = DEFAULT_RETRIES,\n    backoff: float = DEFAULT_BACKOFF,\n    keep_sra: bool = False,\n    verbose: bool = True,\n) -&gt; dict[str, bool]:\n    \"\"\"Download every run of the experiment, parallel at the run level.\n\n    Creates :attr:`srx_dir`, then downloads each run's gzipped FASTQ into it.\n    Runs with an existing ``.&lt;SRR&gt;.success`` flag are skipped; a run that fails\n    is recorded as ``False`` without aborting the others.\n\n    Parameters\n    ----------\n    n_parallel : int, default 1\n        Maximum runs to download concurrently. On a flaky link, fewer\n        concurrent connections (``1`` or ``2``) is often more reliable.\n    max_size : str, default :data:`DEFAULT_MAX_SIZE`\n        Passed to ``prefetch --max-size``; raise it for large runs (``prefetch``\n        otherwise refuses anything over its 20G default).\n    retries : int, default :data:`DEFAULT_RETRIES`\n        Attempts for the resumable ``prefetch`` network step per run; a retry\n        continues the partial download rather than restarting.\n    backoff : float, default :data:`DEFAULT_BACKOFF`\n        Base seconds for the linear backoff between ``prefetch`` retries.\n    keep_sra : bool, default False\n        Keep each run's downloaded ``.sra`` next to its FASTQ (as\n        ``&lt;SRX&gt;/&lt;SRR&gt;.sra``) instead of deleting it after extraction.\n    verbose : bool, default True\n        Print a download plan up front and one line per finished run to\n        ``stderr``. Set ``False`` to download silently.\n\n    Returns\n    -------\n    dict[str, bool]\n        Maps each run accession to whether its data is present on completion.\n    \"\"\"\n    srx_dir = self.srx_dir\n    srx_dir.mkdir(parents=True, exist_ok=True)\n    tasks = [(run, srx_dir) for run in self.experiment.runs]\n    return _run_plan(\n        self.experiment.accession,\n        tasks,\n        n_parallel,\n        output_root=srx_dir,\n        verbose=verbose,\n        max_size=max_size,\n        retries=retries,\n        backoff=backoff,\n        keep_sra=keep_sra,\n    )\n</code></pre>"},{"location":"reference/#labdata.FileLocation","title":"FileLocation  <code>dataclass</code>","text":"<pre><code>FileLocation(service: str, region: str | None, link: str)\n</code></pre> <p>One place a :class:<code>RemoteFile</code> can be fetched from.</p> <p>Parameters:</p> Name Type Description Default <code>service</code> <code>str</code> <p>The hosting service, e.g. <code>\"s3\"</code>, <code>\"gs\"</code>, or <code>\"ncbi\"</code>.</p> required <code>region</code> <code>str or None</code> <p>The cloud region (e.g. <code>\"us-east-1\"</code>), or <code>None</code> when unspecified.</p> required <code>link</code> <code>str</code> <p>The download URL or cloud URI for this location.</p> required"},{"location":"reference/#labdata.RemoteFile","title":"RemoteFile  <code>dataclass</code>","text":"<pre><code>RemoteFile(\n    name: str,\n    type: str,\n    size: int | None,\n    md5: str | None,\n    modification_date: str | None,\n    accession: str,\n    locations: tuple[FileLocation, ...],\n)\n</code></pre> <p>A single file SDL lists for a run, in any of its locations.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The file name, e.g. <code>\"possorted_genome_bam_TC2_d15_1.bam\"</code>.</p> required <code>type</code> <code>str</code> <p>SDL's file type, e.g. <code>\"sra\"</code> for the normalized archive or <code>\"TenX\"</code>, <code>\"bam\"</code>, <code>\"fastq\"</code>, \u2026 for an original/submitted file.</p> required <code>size</code> <code>int or None</code> <p>File size in bytes, or <code>None</code> when unreported.</p> required <code>md5</code> <code>str or None</code> <p>The file's MD5 checksum, or <code>None</code> when unreported.</p> required <code>modification_date</code> <code>str or None</code> <p>The file's modification timestamp (ISO 8601 string), or <code>None</code>.</p> required <code>accession</code> <code>str</code> <p>The run accession this file belongs to.</p> required <code>locations</code> <code>tuple of FileLocation</code> <p>Every place the file can be fetched from, in SDL's order.</p> required"},{"location":"reference/#labdata.RemoteFile.is_sra_archive","title":"is_sra_archive  <code>property</code>","text":"<pre><code>is_sra_archive: bool\n</code></pre> <p>Whether this is a normalized SRA-archive file (vs. an original/submitted one).</p>"},{"location":"reference/#labdata.RemoteFile.url","title":"url  <code>property</code>","text":"<pre><code>url: str\n</code></pre> <p>The first location's download link, or <code>\"\"</code> when there are none.</p> <p>SDL lists the anonymous, worldwide-egress HTTPS link first, so this is the simplest URL to fetch the file from.</p>"},{"location":"reference/#labdata.SdlClient","title":"SdlClient","text":"<pre><code>SdlClient(\n    base_url: str = SDL_RETRIEVE_URL,\n    *,\n    timeout: float = 30.0,\n)\n</code></pre> <p>Entry point to NCBI's SRA Data Locator.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>The <code>retrieve</code> endpoint to query (default :data:<code>SDL_RETRIEVE_URL</code>).</p> <code>SDL_RETRIEVE_URL</code> <code>timeout</code> <code>float</code> <p>Per-request socket timeout, in seconds.</p> <code>30.0</code> Source code in <code>src/labdata/ncbi/sdl.py</code> <pre><code>def __init__(self, base_url: str = SDL_RETRIEVE_URL, *, timeout: float = 30.0) -&gt; None:\n    self.base_url = base_url\n    self.timeout = timeout\n    # Per-instance response cache keyed by accession; redundant lookups across\n    # records that share this client are served without a request.\n    self._cache: dict[str, list[RemoteFile]] = {}\n</code></pre>"},{"location":"reference/#labdata.SdlClient.clear_cache","title":"clear_cache","text":"<pre><code>clear_cache() -&gt; None\n</code></pre> <p>Drop all cached responses (forcing fresh requests on the next call).</p> Source code in <code>src/labdata/ncbi/sdl.py</code> <pre><code>def clear_cache(self) -&gt; None:\n    \"\"\"Drop all cached responses (forcing fresh requests on the next call).\"\"\"\n    self._cache.clear()\n</code></pre>"},{"location":"reference/#labdata.SdlClient.retrieve","title":"retrieve","text":"<pre><code>retrieve(accession: str) -&gt; list[RemoteFile]\n</code></pre> <p>Return every file SDL lists for <code>accession</code>.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>An SRA run accession, e.g. <code>\"SRR20172067\"</code>.</p> required <p>Returns:</p> Type Description <code>list of RemoteFile</code> <p>Both the normalized SRA archive file(s) and any original/submitted files (e.g. a 10X <code>possorted_genome_bam</code>). Cached per client.</p> <p>Raises:</p> Type Description <code>SdlError</code> <p>If the request fails, the response is not valid JSON, or SDL reports a non-OK status for the accession (e.g. unknown or non-public run).</p> Source code in <code>src/labdata/ncbi/sdl.py</code> <pre><code>def retrieve(self, accession: str) -&gt; list[RemoteFile]:\n    \"\"\"Return every file SDL lists for ``accession``.\n\n    Parameters\n    ----------\n    accession : str\n        An SRA run accession, e.g. ``\"SRR20172067\"``.\n\n    Returns\n    -------\n    list of RemoteFile\n        Both the normalized SRA archive file(s) and any original/submitted\n        files (e.g. a 10X ``possorted_genome_bam``). Cached per client.\n\n    Raises\n    ------\n    SdlError\n        If the request fails, the response is not valid JSON, or SDL reports a\n        non-OK status for the accession (e.g. unknown or non-public run).\n    \"\"\"\n    cached = self._cache.get(accession)\n    if cached is None:\n        payload = self._fetch(accession)\n        cached = self._cache[accession] = _parse_files(payload, accession)\n    return list(cached)\n</code></pre>"},{"location":"reference/#labdata.geo.records","title":"records","text":"<p>The GEO object model: Series, Sample, Platform, Experiment, Run, BioProject.</p> <p>Each class is a lazy handle on one accessioned record. Construction only validates and stores the accession; properties issue their NCBI request on first access and cache it (<code>functools.cached_property</code>), so building a record is cheap and network traffic happens only for the fields you read.</p> <p>Three families share most of the plumbing:</p> <ul> <li>GEO records (<code>GSE</code>/<code>GSM</code>/<code>GPL</code>) live in the Entrez <code>gds</code> database   (:class:<code>_GdsRecord</code>). They expose a <code>url</code> and a <code>suppl/</code> directory.</li> <li>SRA records (<code>SRX</code>/<code>SRR</code>) live in the Entrez <code>sra</code> database   (:class:<code>_SraRecord</code>), whose summary carries the experiment/run XML.</li> <li>BioProject (<code>PRJNA</code>/<code>PRJEB</code>/<code>PRJDB</code>) lives in the Entrez   <code>bioproject</code> database (:class:<code>BioProject</code>); it is the umbrella a Series   links out to.</li> </ul> <p>Properties that reference other GEO/SRA objects return instances of the relevant class (sharing this record's Entrez client), not bare accession strings.</p> <p>All classes are gathered in one module because they reference each other mutually (a Series yields Samples; a Sample points back at its Series); a single module sidesteps the import cycle that separate files would create.</p>"},{"location":"reference/#labdata.geo.records.Series","title":"Series","text":"<pre><code>Series(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_GdsRecord</code></p> <p>A GEO Series (<code>GSE000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A GEO Series accession, e.g. <code>\"GSE131907\"</code> (<code>^GSE\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed GEO Series accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.geo.records.Series.summary","title":"summary  <code>property</code>","text":"<pre><code>summary: str\n</code></pre> <p>The Series summary/abstract text.</p>"},{"location":"reference/#labdata.geo.records.Series.pubmed_id","title":"pubmed_id  <code>cached</code> <code>property</code>","text":"<pre><code>pubmed_id: str | None\n</code></pre> <p>The PubMed ID of the associated publication, or <code>None</code>.</p>"},{"location":"reference/#labdata.geo.records.Series.samples","title":"samples  <code>cached</code> <code>property</code>","text":"<pre><code>samples: list[Sample]\n</code></pre> <p>The samples (<code>GSM</code>) belonging to this Series, as :class:<code>Sample</code> instances.</p>"},{"location":"reference/#labdata.geo.records.Series.platforms","title":"platforms  <code>cached</code> <code>property</code>","text":"<pre><code>platforms: list[Platform]\n</code></pre> <p>The platforms (<code>GPL</code>) this Series uses, as :class:<code>Platform</code> instances.</p>"},{"location":"reference/#labdata.geo.records.Series.experiments","title":"experiments  <code>cached</code> <code>property</code>","text":"<pre><code>experiments: list[Experiment]\n</code></pre> <p>The SRA experiments (<code>SRX</code>) linked to this Series, as :class:<code>Experiment</code> instances.</p>"},{"location":"reference/#labdata.geo.records.Series.bioprojects","title":"bioprojects  <code>cached</code> <code>property</code>","text":"<pre><code>bioprojects: list[BioProject]\n</code></pre> <p>The BioProjects (<code>PRJNA\u2026</code>) linked to this Series, as :class:<code>BioProject</code> instances.</p> <p>One <code>elink</code> plus one batched <code>esummary</code>; each :class:<code>BioProject</code> is seeded with the UID and summary already fetched.</p>"},{"location":"reference/#labdata.geo.records.Series.make_sra_run_table","title":"make_sra_run_table","text":"<pre><code>make_sra_run_table() -&gt; pandas.DataFrame\n</code></pre> <p>Build a tidy, run-level (<code>SRR</code>) table for this Series, like SRA Run Selector.</p> <p>Resolves the Series' SRA experiments and fetches NCBI's <code>runinfo</code> report in one batched EFetch per chunk, returning one row per sequencing run. The columns are SRA's <code>runinfo</code> fields (<code>Run</code>, <code>BioSample</code>, <code>Experiment</code>, <code>LibraryName</code>, <code>Platform</code>, \u2026) with a few renamed to the Run-Selector names (<code>AvgSpotLen</code>, <code>Bases</code>, <code>Instrument</code>). The reported size is <code>size_MB</code> \u2014 E-utilities <code>runinfo</code> does not expose an exact byte count.</p> <p>A <code>ReadStructure</code> column (<code>{L1}+{L2}+\u2026</code>, e.g. <code>\"28+94\"</code>) is added from each run's per-read <code>&lt;Statistics&gt;</code>, read from the full SRA XML with a second batched EFetch per chunk (see :attr:<code>Run.read_structure</code>); it is empty for runs SRA reports no read statistics for.</p> <p>Returns:</p> Type Description <code>DataFrame</code> <p>One row per run, key columns ordered first. Empty when the Series has no linked SRA runs.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; table = Series(\"GSE229022\").make_sra_run_table()\n&gt;&gt;&gt; table[[\"Run\", \"BioSample\", \"Instrument\"]].head()\n</code></pre> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def make_sra_run_table(self) -&gt; pandas.DataFrame:\n    \"\"\"Build a tidy, run-level (``SRR``) table for this Series, like SRA Run Selector.\n\n    Resolves the Series' SRA experiments and fetches NCBI's ``runinfo`` report\n    in one batched EFetch per chunk, returning one row per sequencing run. The\n    columns are SRA's ``runinfo`` fields (``Run``, ``BioSample``, ``Experiment``,\n    ``LibraryName``, ``Platform``, \u2026) with a few renamed to the Run-Selector\n    names (``AvgSpotLen``, ``Bases``, ``Instrument``). The reported size is\n    ``size_MB`` \u2014 E-utilities ``runinfo`` does not expose an exact byte count.\n\n    A ``ReadStructure`` column (``{L1}+{L2}+\u2026``, e.g. ``\"28+94\"``) is added\n    from each run's per-read ``&lt;Statistics&gt;``, read from the full SRA XML with\n    a second batched EFetch per chunk (see :attr:`Run.read_structure`); it is\n    empty for runs SRA reports no read statistics for.\n\n    Returns\n    -------\n    pandas.DataFrame\n        One row per run, key columns ordered first. Empty when the Series has\n        no linked SRA runs.\n\n    Examples\n    --------\n    &gt;&gt;&gt; table = Series(\"GSE229022\").make_sra_run_table()  # doctest: +SKIP\n    &gt;&gt;&gt; table[[\"Run\", \"BioSample\", \"Instrument\"]].head()  # doctest: +SKIP\n    \"\"\"\n    import pandas\n\n    sra_uids = self.client.elink(dbfrom=\"gds\", db=\"sra\", uid=self.uid)\n    frames: list[pandas.DataFrame] = []\n    structures: dict[str, str] = {}\n    for chunk in _chunked(sra_uids, _RUNINFO_CHUNK):\n        text = self.client.efetch(db=\"sra\", ids=chunk, rettype=\"runinfo\", retmode=\"text\")\n        if text.strip():\n            frames.append(pandas.read_csv(io.StringIO(text)))\n        xml = self.client.efetch(db=\"sra\", ids=chunk, rettype=\"full\", retmode=\"xml\")\n        for accession, block in _run_blocks(xml).items():\n            structures[accession] = _read_structure(_read_lengths(block))\n    if not frames:\n        return pandas.DataFrame()\n    table = pandas.concat(frames, ignore_index=True).rename(columns=_RUNINFO_RENAME)\n    table[\"ReadStructure\"] = [structures.get(run, \"\") for run in table[\"Run\"]]\n    leading = [column for column in _RUN_TABLE_COLUMNS if column in table.columns]\n    remaining = [column for column in table.columns if column not in leading]\n    return table.reindex(columns=leading + remaining)\n</code></pre>"},{"location":"reference/#labdata.geo.records.Series.download","title":"download","text":"<pre><code>download(\n    output_dir: str | Path = \".\",\n    n_parallel: int = 1,\n    *,\n    max_size: str | None = None,\n    retries: int | None = None,\n    backoff: float | None = None,\n    keep_sra: bool = False,\n    verbose: bool = True,\n) -&gt; dict[str, bool]\n</code></pre> <p>Download FASTQ for every run of every SRA experiment in this Series.</p> <p>Lays the data out as <code>&lt;output_dir&gt;/&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;*.fastq.gz</code> \u2014 one directory per experiment under a directory named for this Series \u2014 via sra-tools (<code>prefetch</code> + <code>fasterq-dump</code>). Runs already marked done (a <code>.&lt;SRR&gt;.success</code> flag) are skipped; the whole Series is downloaded in parallel at the run (<code>SRR</code>) level. The resumable <code>prefetch</code> step is retried on failure, so this is safe to rerun on an unstable connection.</p> <p>Parameters:</p> Name Type Description Default <code>output_dir</code> <code>str or Path</code> <p>Parent directory; a <code>&lt;output_dir&gt;/&lt;GSE&gt;</code> subtree is created for this Series.</p> <code>\".\"</code> <code>n_parallel</code> <code>int</code> <p>Maximum runs to download concurrently across the whole Series. On a flaky link, fewer concurrent connections (<code>1</code> or <code>2</code>) is often more reliable.</p> <code>1</code> <code>max_size</code> <code>str</code> <p>Passed to <code>prefetch --max-size</code> (defaults to :data:<code>~labdata.geo.sratools.DEFAULT_MAX_SIZE</code>); raise it for runs that exceed <code>prefetch</code>'s 20G default.</p> <code>None</code> <code>retries</code> <code>int</code> <p>Attempts for the resumable <code>prefetch</code> network step per run (defaults to :data:<code>~labdata.geo.sratools.DEFAULT_RETRIES</code>).</p> <code>None</code> <code>backoff</code> <code>float</code> <p>Base seconds for the linear backoff between <code>prefetch</code> retries (defaults to :data:<code>~labdata.geo.sratools.DEFAULT_BACKOFF</code>).</p> <code>None</code> <code>keep_sra</code> <code>bool</code> <p>Keep each run's downloaded <code>.sra</code> next to its FASTQ (as <code>&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;.sra</code>) instead of deleting it after extraction.</p> <code>False</code> <code>verbose</code> <code>bool</code> <p>Print a download plan up front and one line per finished run to <code>stderr</code>. Set <code>False</code> to download silently.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, bool]</code> <p>Maps each run accession to whether its data is present on completion.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; Series(\"GSE229022\").download(\"./fastq\", n_parallel=4)\n{'SRR24084454': True, 'SRR24084455': True, ...}\n</code></pre> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def download(\n    self,\n    output_dir: str | Path = \".\",\n    n_parallel: int = 1,\n    *,\n    max_size: str | None = None,\n    retries: int | None = None,\n    backoff: float | None = None,\n    keep_sra: bool = False,\n    verbose: bool = True,\n) -&gt; dict[str, bool]:\n    \"\"\"Download FASTQ for every run of every SRA experiment in this Series.\n\n    Lays the data out as ``&lt;output_dir&gt;/&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;*.fastq.gz`` \u2014 one\n    directory per experiment under a directory named for this Series \u2014 via\n    sra-tools (``prefetch`` + ``fasterq-dump``). Runs already marked done (a\n    ``.&lt;SRR&gt;.success`` flag) are skipped; the whole Series is downloaded in\n    parallel at the run (``SRR``) level. The resumable ``prefetch`` step is\n    retried on failure, so this is safe to rerun on an unstable connection.\n\n    Parameters\n    ----------\n    output_dir : str or Path, default \".\"\n        Parent directory; a ``&lt;output_dir&gt;/&lt;GSE&gt;`` subtree is created for this Series.\n    n_parallel : int, default 1\n        Maximum runs to download concurrently across the whole Series. On a\n        flaky link, fewer concurrent connections (``1`` or ``2``) is often\n        more reliable.\n    max_size : str, optional\n        Passed to ``prefetch --max-size`` (defaults to\n        :data:`~labdata.geo.sratools.DEFAULT_MAX_SIZE`); raise it for runs that\n        exceed ``prefetch``'s 20G default.\n    retries : int, optional\n        Attempts for the resumable ``prefetch`` network step per run (defaults\n        to :data:`~labdata.geo.sratools.DEFAULT_RETRIES`).\n    backoff : float, optional\n        Base seconds for the linear backoff between ``prefetch`` retries\n        (defaults to :data:`~labdata.geo.sratools.DEFAULT_BACKOFF`).\n    keep_sra : bool, default False\n        Keep each run's downloaded ``.sra`` next to its FASTQ (as\n        ``&lt;GSE&gt;/&lt;SRX&gt;/&lt;SRR&gt;.sra``) instead of deleting it after extraction.\n    verbose : bool, default True\n        Print a download plan up front and one line per finished run to\n        ``stderr``. Set ``False`` to download silently.\n\n    Returns\n    -------\n    dict[str, bool]\n        Maps each run accession to whether its data is present on completion.\n\n    Examples\n    --------\n    &gt;&gt;&gt; Series(\"GSE229022\").download(\"./fastq\", n_parallel=4)  # doctest: +SKIP\n    {'SRR24084454': True, 'SRR24084455': True, ...}\n    \"\"\"\n    from labdata.geo import sratools\n\n    kwargs = {\n        \"max_size\": max_size,\n        \"retries\": retries,\n        \"backoff\": backoff,\n    }\n    overrides = {key: value for key, value in kwargs.items() if value is not None}\n\n    base = Path(output_dir) / self.accession\n    tasks: list[tuple[Run, Path]] = []\n    for experiment in self.experiments:\n        srx_dir = base / experiment.accession\n        srx_dir.mkdir(parents=True, exist_ok=True)\n        tasks.extend((run, srx_dir) for run in experiment.runs)\n    return sratools._run_plan(\n        self.accession,\n        tasks,\n        n_parallel,\n        output_root=base,\n        verbose=verbose,\n        keep_sra=keep_sra,\n        **overrides,\n    )\n</code></pre>"},{"location":"reference/#labdata.geo.records.Sample","title":"Sample","text":"<pre><code>Sample(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_GdsRecord</code></p> <p>A GEO Sample (<code>GSM000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A GEO Sample accession, e.g. <code>\"GSM3827114\"</code> (<code>^GSM\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed GEO Sample accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.geo.records.Sample.series","title":"series  <code>property</code>","text":"<pre><code>series: Series | None\n</code></pre> <p>The parent :class:<code>Series</code>, or <code>None</code> if the summary lists none.</p>"},{"location":"reference/#labdata.geo.records.Sample.platform","title":"platform  <code>property</code>","text":"<pre><code>platform: Platform | None\n</code></pre> <p>The :class:<code>Platform</code> this sample was run on, or <code>None</code>.</p>"},{"location":"reference/#labdata.geo.records.Sample.experiments","title":"experiments  <code>cached</code> <code>property</code>","text":"<pre><code>experiments: list[Experiment]\n</code></pre> <p>The SRA experiments (<code>SRX</code>) for this sample, as :class:<code>Experiment</code> instances.</p>"},{"location":"reference/#labdata.geo.records.Platform","title":"Platform","text":"<pre><code>Platform(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_GdsRecord</code></p> <p>A GEO Platform (<code>GPL000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A GEO Platform accession, e.g. <code>\"GPL16791\"</code> (<code>^GPL\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed GEO Platform accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.geo.records.Platform.sample_count","title":"sample_count  <code>property</code>","text":"<pre><code>sample_count: int | None\n</code></pre> <p>The number of samples GEO has on this platform, or <code>None</code> if unknown.</p>"},{"location":"reference/#labdata.geo.records.Experiment","title":"Experiment","text":"<pre><code>Experiment(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_SraRecord</code></p> <p>An SRA experiment (<code>SRX000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>An SRA experiment accession, e.g. <code>\"SRX5921017\"</code> (<code>^[SED]RX\\d+$</code>, covering SRA/ENA/DDBJ).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed experiment accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.geo.records.Experiment.title","title":"title  <code>property</code>","text":"<pre><code>title: str\n</code></pre> <p>The experiment title.</p>"},{"location":"reference/#labdata.geo.records.Experiment.study","title":"study  <code>property</code>","text":"<pre><code>study: str\n</code></pre> <p>The parent study accession (<code>SRP\u2026</code>), or empty string if absent.</p>"},{"location":"reference/#labdata.geo.records.Experiment.runs","title":"runs  <code>cached</code> <code>property</code>","text":"<pre><code>runs: list[Run]\n</code></pre> <p>The sequencing runs (<code>SRR</code>) of this experiment, as :class:<code>Run</code> instances.</p> <p>A run resolves to the same <code>sra</code> record as its experiment, so each :class:<code>Run</code> is seeded with this experiment's summary (no extra fetch).</p>"},{"location":"reference/#labdata.geo.records.Run","title":"Run","text":"<pre><code>Run(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_SraRecord</code></p> <p>An SRA run (<code>SRR000000</code>), resolved lazily through NCBI Entrez.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>An SRA run accession, e.g. <code>\"SRR9000001\"</code> (<code>^[SED]RR\\d+$</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed run accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.geo.records.Run.total_spots","title":"total_spots  <code>property</code>","text":"<pre><code>total_spots: int | None\n</code></pre> <p>The number of spots (reads) in this run, or <code>None</code> if unreported.</p>"},{"location":"reference/#labdata.geo.records.Run.total_bases","title":"total_bases  <code>property</code>","text":"<pre><code>total_bases: int | None\n</code></pre> <p>The number of bases in this run, or <code>None</code> if unreported.</p>"},{"location":"reference/#labdata.geo.records.Run.is_public","title":"is_public  <code>property</code>","text":"<pre><code>is_public: bool\n</code></pre> <p>Whether the run's data is publicly accessible.</p>"},{"location":"reference/#labdata.geo.records.Run.read_lengths","title":"read_lengths  <code>property</code>","text":"<pre><code>read_lengths: list[int]\n</code></pre> <p>The average length of each read (mate) sequenced per spot, in read order.</p> <p>Reads SRA's per-run <code>&lt;Statistics&gt;</code> from the full record (one EFetch, cached). A paired run yields two lengths, e.g. <code>[28, 94]</code>; a single-end run yields one. Empty when SRA reports no read statistics.</p>"},{"location":"reference/#labdata.geo.records.Run.n_reads","title":"n_reads  <code>property</code>","text":"<pre><code>n_reads: int | None\n</code></pre> <p>The number of reads (mates) sequenced per spot, or <code>None</code> if unreported.</p>"},{"location":"reference/#labdata.geo.records.Run.read_structure","title":"read_structure  <code>property</code>","text":"<pre><code>read_structure: str\n</code></pre> <p>The run's read structure as <code>{L1}+{L2}+\u2026</code> (e.g. <code>\"28+94\"</code>), or <code>\"\"</code>.</p> <p>A compact rendering of :attr:<code>read_lengths</code>: each read's rounded average length joined with <code>+</code> in read order. Empty when the lengths are unknown.</p>"},{"location":"reference/#labdata.geo.records.Run.experiment","title":"experiment  <code>cached</code> <code>property</code>","text":"<pre><code>experiment: Experiment\n</code></pre> <p>The parent :class:<code>Experiment</code> (<code>SRX</code>) of this run.</p> <p>Shares this run's <code>sra</code> record, so the experiment is seeded from the same summary (no extra fetch).</p>"},{"location":"reference/#labdata.geo.records.Run.files","title":"files  <code>cached</code> <code>property</code>","text":"<pre><code>files: list[RemoteFile]\n</code></pre> <p>Every file the SRA Data Locator lists for this run (one SDL request, cached).</p> <p>Includes both the normalized SRA archive file(s) and any original/submitted files (e.g. a 10X Genomics <code>possorted_genome_bam</code>). This is the data behind the Run Browser's \"Data access\" panel.</p> <p>Returns:</p> Type Description <code>list of RemoteFile</code> <p>All files SDL exposes for this run, each with its download locations.</p> <p>Raises:</p> Type Description <code>SdlError</code> <p>If the SDL request fails or the run is unknown/non-public.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; [(f.name, f.type) for f in Run(\"SRR20172067\").files]\n[('possorted_genome_bam_TC2_d15_1.bam', 'TenX'), ('SRR20172067', 'sra')]\n</code></pre>"},{"location":"reference/#labdata.geo.records.Run.original_files","title":"original_files  <code>property</code>","text":"<pre><code>original_files: list[RemoteFile]\n</code></pre> <p>The original/submitted files for this run (the \"Original format\" listing).</p> <p>The submitter's own uploads \u2014 e.g. the 10X Genomics BAM behind a run whose <code>.sra</code> only stored a single read \u2014 as opposed to the normalized SRA archive. Filters :attr:<code>files</code> to the non-archive types.</p> <p>Returns:</p> Type Description <code>list of RemoteFile</code> <p>Files whose type is not a normalized SRA archive type; empty when the submission has no original-format files.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; [f.name for f in Run(\"SRR20172067\").original_files]\n['possorted_genome_bam_TC2_d15_1.bam']\n</code></pre>"},{"location":"reference/#labdata.geo.records.BioProject","title":"BioProject","text":"<pre><code>BioProject(\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n)\n</code></pre> <p>               Bases: <code>_Record</code></p> <p>An NCBI BioProject (<code>PRJNA000000</code>), resolved lazily through NCBI Entrez.</p> <p>BioProject is the umbrella record that groups a study's GEO Series and SRA data. It lives in the Entrez <code>bioproject</code> database, whose esummary uses the v2.0 <code>DocumentSummarySet</code> shape.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>A BioProject accession, e.g. <code>\"PRJNA545296\"</code> (<code>^PRJ[A-Z]{2}\\d+$</code>, covering NCBI <code>PRJNA</code>, EBI <code>PRJEB</code>, and DDBJ <code>PRJDB</code>).</p> required <code>client</code> <code>EntrezClient or None</code> <p>Entrez client to use; a default one is built on first network access.</p> <code>None</code> <p>Raises:</p> Type Description <code>AccessionError</code> <p>If <code>accession</code> is not a well-formed BioProject accession.</p> Source code in <code>src/labdata/geo/records.py</code> <pre><code>def __init__(\n    self,\n    accession: str,\n    *,\n    client: EntrezClient | None = None,\n    sdl_client: SdlClient | None = None,\n) -&gt; None:\n    accession = accession.strip().upper()\n    if not isinstance(accession, str) or not self._ACCESSION_RE.match(accession):\n        raise AccessionError(f\"not a GEO/SRA {self._KIND} accession: {accession!r}\")\n    self.accession = accession\n    self._client = client\n    self._sdl_client = sdl_client\n</code></pre>"},{"location":"reference/#labdata.geo.records.BioProject.uid","title":"uid  <code>cached</code> <code>property</code>","text":"<pre><code>uid: str\n</code></pre> <p>The Entrez <code>bioproject</code> UID for this record.</p> <p>Raises:</p> Type Description <code>EntrezError</code> <p>If NCBI returns no matching <code>bioproject</code> record.</p>"},{"location":"reference/#labdata.geo.records.BioProject.title","title":"title  <code>property</code>","text":"<pre><code>title: str\n</code></pre> <p>The project title.</p>"},{"location":"reference/#labdata.geo.records.BioProject.name","title":"name  <code>property</code>","text":"<pre><code>name: str\n</code></pre> <p>The project name.</p>"},{"location":"reference/#labdata.geo.records.BioProject.description","title":"description  <code>property</code>","text":"<pre><code>description: str\n</code></pre> <p>The free-text project description.</p>"},{"location":"reference/#labdata.geo.records.BioProject.organism","title":"organism  <code>property</code>","text":"<pre><code>organism: str\n</code></pre> <p>The organism the project targets.</p>"},{"location":"reference/#labdata.geo.records.BioProject.data_type","title":"data_type  <code>property</code>","text":"<pre><code>data_type: str\n</code></pre> <p>The project data type (e.g. <code>Transcriptome or Gene expression</code>).</p>"},{"location":"reference/#labdata.geo.records.BioProject.registration_date","title":"registration_date  <code>property</code>","text":"<pre><code>registration_date: str\n</code></pre> <p>The project's registration date as reported by NCBI.</p>"},{"location":"reference/#labdata.geo.records.BioProject.submitter","title":"submitter  <code>property</code>","text":"<pre><code>submitter: str\n</code></pre> <p>The submitting organization.</p>"},{"location":"reference/#labdata.geo.records.BioProject.url","title":"url  <code>property</code>","text":"<pre><code>url: str\n</code></pre> <p>The NCBI BioProject web URL for this record.</p>"},{"location":"reference/#labdata.geo.records.BioProject.series","title":"series  <code>cached</code> <code>property</code>","text":"<pre><code>series: list[Series]\n</code></pre> <p>The GEO Series (<code>GSE</code>) under this project, as :class:<code>Series</code> instances.</p> <p>One <code>elink</code> plus one batched <code>esummary</code>; each :class:<code>Series</code> is seeded with the UID and summary already fetched.</p>"},{"location":"reference/#labdata.geo.records.BioProject.experiments","title":"experiments  <code>cached</code> <code>property</code>","text":"<pre><code>experiments: list[Experiment]\n</code></pre> <p>The SRA experiments (<code>SRX</code>) under this project, as :class:<code>Experiment</code> instances.</p>"},{"location":"reference/#labdata.ncbi.entrez","title":"entrez","text":"<p>A thin, mockable wrapper over Biopython's :mod:<code>Bio.Entrez</code>.</p> <p>Every NCBI E-utilities call in liulab-data goes through :class:<code>EntrezClient</code>, which sets the contact email / API key from :func:<code>labdata.ncbi.config.load_credentials</code> and exposes small typed helpers (<code>esearch</code>, <code>esummary</code>, <code>esummary_many</code>, <code>elink</code>) that parse the XML handle into plain Python structures, plus <code>efetch</code> for raw (e.g. CSV) responses. Concentrating network access in one seam keeps the rest of the package easy to unit-test by substituting a fake client.</p> <p>Responses are memoized per client instance, so records that share a client never repeat an identical request; <code>esummary_many</code> further collapses a list of UIDs into one batched request.</p>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient","title":"EntrezClient","text":"<pre><code>EntrezClient(\n    credentials: NcbiCredentials | None = None,\n    *,\n    allow_prompt: bool = True,\n)\n</code></pre> <p>Authenticated entry point to NCBI E-utilities.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>NcbiCredentials or None</code> <p>Credentials to use. When <code>None</code> (default) they are resolved via :func:<code>labdata.ncbi.config.load_credentials</code>.</p> <code>None</code> <code>allow_prompt</code> <code>bool</code> <p>Forwarded to :func:<code>load_credentials</code> when <code>credentials</code> is <code>None</code>.</p> <code>True</code> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def __init__(\n    self,\n    credentials: NcbiCredentials | None = None,\n    *,\n    allow_prompt: bool = True,\n) -&gt; None:\n    self.credentials = credentials or load_credentials(allow_prompt=allow_prompt)\n    _entrez.email = self.credentials.email\n    if self.credentials.api_key:\n        _entrez.api_key = self.credentials.api_key\n    # Per-instance response cache keyed by call signature. Redundant lookups\n    # across records that share this client are served without a request.\n    self._cache: dict[tuple[Any, ...], Any] = {}\n</code></pre>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient.clear_cache","title":"clear_cache","text":"<pre><code>clear_cache() -&gt; None\n</code></pre> <p>Drop all cached responses (forcing fresh requests on the next call).</p> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def clear_cache(self) -&gt; None:\n    \"\"\"Drop all cached responses (forcing fresh requests on the next call).\"\"\"\n    self._cache.clear()\n</code></pre>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient.esearch","title":"esearch","text":"<pre><code>esearch(\n    db: str, term: str, *, retmax: int = 20\n) -&gt; list[str]\n</code></pre> <p>Run an ESearch and return the matching UIDs.</p> <p>Parameters:</p> Name Type Description Default <code>db</code> <code>str</code> <p>Entrez database to search (e.g. <code>\"gds\"</code>).</p> required <code>term</code> <code>str</code> <p>The search term.</p> required <code>retmax</code> <code>int</code> <p>Maximum number of UIDs to return.</p> <code>20</code> <p>Returns:</p> Type Description <code>list of str</code> <p>The matching UIDs, in NCBI's ranking order. Cached per client.</p> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def esearch(self, db: str, term: str, *, retmax: int = 20) -&gt; list[str]:\n    \"\"\"Run an ESearch and return the matching UIDs.\n\n    Parameters\n    ----------\n    db : str\n        Entrez database to search (e.g. ``\"gds\"``).\n    term : str\n        The search term.\n    retmax : int\n        Maximum number of UIDs to return.\n\n    Returns\n    -------\n    list of str\n        The matching UIDs, in NCBI's ranking order. Cached per client.\n    \"\"\"\n    key = (\"esearch\", db, term, retmax)\n    cached = self._cache.get(key)\n    if cached is None:\n        record = self._read(_entrez.esearch(db=db, term=term, retmax=retmax))\n        cached = self._cache[key] = [str(uid) for uid in record.get(\"IdList\", [])]\n    return list(cached)\n</code></pre>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient.esummary","title":"esummary","text":"<pre><code>esummary(db: str, uid: str) -&gt; dict[str, Any]\n</code></pre> <p>Fetch the document summary for a single UID.</p> <p>Handles both esummary response shapes: the classic <code>DocSum</code> list used by <code>gds</code>/<code>sra</code> and the v2.0 <code>DocumentSummarySet</code> used by databases such as <code>bioproject</code>.</p> <p>Parameters:</p> Name Type Description Default <code>db</code> <code>str</code> <p>Entrez database (e.g. <code>\"gds\"</code>, <code>\"sra\"</code>, or <code>\"bioproject\"</code>).</p> required <code>uid</code> <code>str</code> <p>The UID whose summary to fetch.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>The document summary as a plain dictionary. Cached per client.</p> <p>Raises:</p> Type Description <code>EntrezError</code> <p>If NCBI returns no summary for the UID.</p> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def esummary(self, db: str, uid: str) -&gt; dict[str, Any]:\n    \"\"\"Fetch the document summary for a single UID.\n\n    Handles both esummary response shapes: the classic ``DocSum`` list used\n    by ``gds``/``sra`` and the v2.0 ``DocumentSummarySet`` used by databases\n    such as ``bioproject``.\n\n    Parameters\n    ----------\n    db : str\n        Entrez database (e.g. ``\"gds\"``, ``\"sra\"``, or ``\"bioproject\"``).\n    uid : str\n        The UID whose summary to fetch.\n\n    Returns\n    -------\n    dict\n        The document summary as a plain dictionary. Cached per client.\n\n    Raises\n    ------\n    EntrezError\n        If NCBI returns no summary for the UID.\n    \"\"\"\n    key = (\"esummary\", db, str(uid))\n    cached = self._cache.get(key)\n    if cached is None:\n        record = self._read(_entrez.esummary(db=db, id=str(uid)))\n        docsums = _docsums(record)\n        if not docsums:\n            raise EntrezError(f\"empty esummary for {db} uid {uid!r}\")\n        cached = self._cache[key] = dict(docsums[0])\n    return cached\n</code></pre>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient.esummary_many","title":"esummary_many","text":"<pre><code>esummary_many(\n    db: str, uids: list[str]\n) -&gt; list[dict[str, Any]]\n</code></pre> <p>Fetch document summaries for several UIDs in a single request.</p> <p>Collapses what would otherwise be one ESummary per UID into one batched call (NCBI accepts a comma-joined id list), chunked to keep the request URL within limits. Use this instead of looping over :meth:<code>esummary</code>.</p> <p>Parameters:</p> Name Type Description Default <code>db</code> <code>str</code> <p>Entrez database (e.g. <code>\"gds\"</code>, <code>\"sra\"</code>, or <code>\"bioproject\"</code>).</p> required <code>uids</code> <code>list of str</code> <p>The UIDs whose summaries to fetch. An empty list issues no request.</p> required <p>Returns:</p> Type Description <code>list of dict</code> <p>One document summary per UID, in NCBI's order. The mappings may carry NCBI's <code>Id</code>/<code>uid</code> so callers can recover each summary's UID via :func:<code>_docsum_uid</code>. Cached per client by the exact UID list.</p> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def esummary_many(self, db: str, uids: list[str]) -&gt; list[dict[str, Any]]:\n    \"\"\"Fetch document summaries for several UIDs in a single request.\n\n    Collapses what would otherwise be one ESummary per UID into one batched\n    call (NCBI accepts a comma-joined id list), chunked to keep the request\n    URL within limits. Use this instead of looping over :meth:`esummary`.\n\n    Parameters\n    ----------\n    db : str\n        Entrez database (e.g. ``\"gds\"``, ``\"sra\"``, or ``\"bioproject\"``).\n    uids : list of str\n        The UIDs whose summaries to fetch. An empty list issues no request.\n\n    Returns\n    -------\n    list of dict\n        One document summary per UID, in NCBI's order. The mappings may carry\n        NCBI's ``Id``/``uid`` so callers can recover each summary's UID via\n        :func:`_docsum_uid`. Cached per client by the exact UID list.\n    \"\"\"\n    ids = [str(uid) for uid in uids]\n    if not ids:\n        return []\n    key = (\"esummary_many\", db, tuple(ids))\n    cached = self._cache.get(key)\n    if cached is None:\n        docsums: list[Any] = []\n        for chunk in _chunked(ids, _ESUMMARY_CHUNK):\n            record = self._read(_entrez.esummary(db=db, id=\",\".join(chunk)))\n            docsums.extend(_docsums(record))\n        cached = self._cache[key] = docsums\n    return list(cached)\n</code></pre>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient.elink","title":"elink","text":"<pre><code>elink(dbfrom: str, db: str, uid: str) -&gt; list[str]\n</code></pre> <p>Return UIDs in <code>db</code> linked from <code>uid</code> in <code>dbfrom</code>.</p> <p>Parameters:</p> Name Type Description Default <code>dbfrom</code> <code>str</code> <p>Source database (e.g. <code>\"gds\"</code>).</p> required <code>db</code> <code>str</code> <p>Target database (e.g. <code>\"sra\"</code>).</p> required <code>uid</code> <code>str</code> <p>The source UID.</p> required <p>Returns:</p> Type Description <code>list of str</code> <p>Linked UIDs in the target database (empty if there are none). Cached per client.</p> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def elink(self, dbfrom: str, db: str, uid: str) -&gt; list[str]:\n    \"\"\"Return UIDs in ``db`` linked from ``uid`` in ``dbfrom``.\n\n    Parameters\n    ----------\n    dbfrom : str\n        Source database (e.g. ``\"gds\"``).\n    db : str\n        Target database (e.g. ``\"sra\"``).\n    uid : str\n        The source UID.\n\n    Returns\n    -------\n    list of str\n        Linked UIDs in the target database (empty if there are none). Cached\n        per client.\n    \"\"\"\n    key = (\"elink\", dbfrom, db, str(uid))\n    cached = self._cache.get(key)\n    if cached is None:\n        record = self._read(_entrez.elink(dbfrom=dbfrom, db=db, id=str(uid)))\n        linked: list[str] = []\n        for linkset in record:\n            for linksetdb in linkset.get(\"LinkSetDb\", []):\n                linked.extend(str(link[\"Id\"]) for link in linksetdb.get(\"Link\", []))\n        cached = self._cache[key] = linked\n    return list(cached)\n</code></pre>"},{"location":"reference/#labdata.ncbi.entrez.EntrezClient.efetch","title":"efetch","text":"<pre><code>efetch(\n    db: str,\n    ids: list[str],\n    *,\n    rettype: str,\n    retmode: str = \"text\",\n) -&gt; str\n</code></pre> <p>Fetch raw records via EFetch and return the response as text.</p> <p>Unlike the other helpers this returns the response unparsed (e.g. the SRA <code>runinfo</code> CSV), because EFetch serves formats Biopython does not parse into Python structures.</p> <p>Parameters:</p> Name Type Description Default <code>db</code> <code>str</code> <p>Entrez database (e.g. <code>\"sra\"</code>).</p> required <code>ids</code> <code>list of str</code> <p>The UIDs to fetch. An empty list issues no request.</p> required <code>rettype</code> <code>str</code> <p>EFetch return type (e.g. <code>\"runinfo\"</code>).</p> required <code>retmode</code> <code>str</code> <p>EFetch return mode (default <code>\"text\"</code>).</p> <code>'text'</code> <p>Returns:</p> Type Description <code>str</code> <p>The raw response body (empty string for an empty <code>ids</code>). Cached per client by the exact request.</p> Source code in <code>src/labdata/ncbi/entrez.py</code> <pre><code>def efetch(\n    self,\n    db: str,\n    ids: list[str],\n    *,\n    rettype: str,\n    retmode: str = \"text\",\n) -&gt; str:\n    \"\"\"Fetch raw records via EFetch and return the response as text.\n\n    Unlike the other helpers this returns the response **unparsed** (e.g. the\n    SRA ``runinfo`` CSV), because EFetch serves formats Biopython does not\n    parse into Python structures.\n\n    Parameters\n    ----------\n    db : str\n        Entrez database (e.g. ``\"sra\"``).\n    ids : list of str\n        The UIDs to fetch. An empty list issues no request.\n    rettype : str\n        EFetch return type (e.g. ``\"runinfo\"``).\n    retmode : str\n        EFetch return mode (default ``\"text\"``).\n\n    Returns\n    -------\n    str\n        The raw response body (empty string for an empty ``ids``). Cached per\n        client by the exact request.\n    \"\"\"\n    uids = [str(uid) for uid in ids]\n    if not uids:\n        return \"\"\n    key = (\"efetch\", db, tuple(uids), rettype, retmode)\n    cached = self._cache.get(key)\n    if cached is None:\n        cached = self._cache[key] = self._read_text(\n            _entrez.efetch(db=db, id=\",\".join(uids), rettype=rettype, retmode=retmode)\n        )\n    return cached\n</code></pre>"},{"location":"reference/#labdata.ncbi.sdl","title":"sdl","text":"<p>A thin, mockable client for NCBI's SRA Data Locator (SDL).</p> <p>The SRA Run Browser's \"Data access\" panel \u2014 both the normalized SRA archive files and the original format files the submitter uploaded \u2014 is served by the SDL service rather than by E-utilities. :class:<code>SdlClient</code> wraps the public <code>sdl/2/retrieve</code> endpoint and returns the file listing as plain :class:<code>RemoteFile</code> objects, so the rest of the package can discover (for example) the original 10X Genomics BAM behind a run whose <code>.sra</code> only stored one read.</p> <p>Concentrating this network access in one seam keeps callers easy to unit-test by substituting a fake client, mirroring :class:<code>labdata.ncbi.entrez.EntrezClient</code>. Responses are memoized per client instance, keyed by accession.</p>"},{"location":"reference/#labdata.ncbi.sdl.FileLocation","title":"FileLocation  <code>dataclass</code>","text":"<pre><code>FileLocation(service: str, region: str | None, link: str)\n</code></pre> <p>One place a :class:<code>RemoteFile</code> can be fetched from.</p> <p>Parameters:</p> Name Type Description Default <code>service</code> <code>str</code> <p>The hosting service, e.g. <code>\"s3\"</code>, <code>\"gs\"</code>, or <code>\"ncbi\"</code>.</p> required <code>region</code> <code>str or None</code> <p>The cloud region (e.g. <code>\"us-east-1\"</code>), or <code>None</code> when unspecified.</p> required <code>link</code> <code>str</code> <p>The download URL or cloud URI for this location.</p> required"},{"location":"reference/#labdata.ncbi.sdl.RemoteFile","title":"RemoteFile  <code>dataclass</code>","text":"<pre><code>RemoteFile(\n    name: str,\n    type: str,\n    size: int | None,\n    md5: str | None,\n    modification_date: str | None,\n    accession: str,\n    locations: tuple[FileLocation, ...],\n)\n</code></pre> <p>A single file SDL lists for a run, in any of its locations.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The file name, e.g. <code>\"possorted_genome_bam_TC2_d15_1.bam\"</code>.</p> required <code>type</code> <code>str</code> <p>SDL's file type, e.g. <code>\"sra\"</code> for the normalized archive or <code>\"TenX\"</code>, <code>\"bam\"</code>, <code>\"fastq\"</code>, \u2026 for an original/submitted file.</p> required <code>size</code> <code>int or None</code> <p>File size in bytes, or <code>None</code> when unreported.</p> required <code>md5</code> <code>str or None</code> <p>The file's MD5 checksum, or <code>None</code> when unreported.</p> required <code>modification_date</code> <code>str or None</code> <p>The file's modification timestamp (ISO 8601 string), or <code>None</code>.</p> required <code>accession</code> <code>str</code> <p>The run accession this file belongs to.</p> required <code>locations</code> <code>tuple of FileLocation</code> <p>Every place the file can be fetched from, in SDL's order.</p> required"},{"location":"reference/#labdata.ncbi.sdl.RemoteFile.is_sra_archive","title":"is_sra_archive  <code>property</code>","text":"<pre><code>is_sra_archive: bool\n</code></pre> <p>Whether this is a normalized SRA-archive file (vs. an original/submitted one).</p>"},{"location":"reference/#labdata.ncbi.sdl.RemoteFile.url","title":"url  <code>property</code>","text":"<pre><code>url: str\n</code></pre> <p>The first location's download link, or <code>\"\"</code> when there are none.</p> <p>SDL lists the anonymous, worldwide-egress HTTPS link first, so this is the simplest URL to fetch the file from.</p>"},{"location":"reference/#labdata.ncbi.sdl.SdlClient","title":"SdlClient","text":"<pre><code>SdlClient(\n    base_url: str = SDL_RETRIEVE_URL,\n    *,\n    timeout: float = 30.0,\n)\n</code></pre> <p>Entry point to NCBI's SRA Data Locator.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>The <code>retrieve</code> endpoint to query (default :data:<code>SDL_RETRIEVE_URL</code>).</p> <code>SDL_RETRIEVE_URL</code> <code>timeout</code> <code>float</code> <p>Per-request socket timeout, in seconds.</p> <code>30.0</code> Source code in <code>src/labdata/ncbi/sdl.py</code> <pre><code>def __init__(self, base_url: str = SDL_RETRIEVE_URL, *, timeout: float = 30.0) -&gt; None:\n    self.base_url = base_url\n    self.timeout = timeout\n    # Per-instance response cache keyed by accession; redundant lookups across\n    # records that share this client are served without a request.\n    self._cache: dict[str, list[RemoteFile]] = {}\n</code></pre>"},{"location":"reference/#labdata.ncbi.sdl.SdlClient.clear_cache","title":"clear_cache","text":"<pre><code>clear_cache() -&gt; None\n</code></pre> <p>Drop all cached responses (forcing fresh requests on the next call).</p> Source code in <code>src/labdata/ncbi/sdl.py</code> <pre><code>def clear_cache(self) -&gt; None:\n    \"\"\"Drop all cached responses (forcing fresh requests on the next call).\"\"\"\n    self._cache.clear()\n</code></pre>"},{"location":"reference/#labdata.ncbi.sdl.SdlClient.retrieve","title":"retrieve","text":"<pre><code>retrieve(accession: str) -&gt; list[RemoteFile]\n</code></pre> <p>Return every file SDL lists for <code>accession</code>.</p> <p>Parameters:</p> Name Type Description Default <code>accession</code> <code>str</code> <p>An SRA run accession, e.g. <code>\"SRR20172067\"</code>.</p> required <p>Returns:</p> Type Description <code>list of RemoteFile</code> <p>Both the normalized SRA archive file(s) and any original/submitted files (e.g. a 10X <code>possorted_genome_bam</code>). Cached per client.</p> <p>Raises:</p> Type Description <code>SdlError</code> <p>If the request fails, the response is not valid JSON, or SDL reports a non-OK status for the accession (e.g. unknown or non-public run).</p> Source code in <code>src/labdata/ncbi/sdl.py</code> <pre><code>def retrieve(self, accession: str) -&gt; list[RemoteFile]:\n    \"\"\"Return every file SDL lists for ``accession``.\n\n    Parameters\n    ----------\n    accession : str\n        An SRA run accession, e.g. ``\"SRR20172067\"``.\n\n    Returns\n    -------\n    list of RemoteFile\n        Both the normalized SRA archive file(s) and any original/submitted\n        files (e.g. a 10X ``possorted_genome_bam``). Cached per client.\n\n    Raises\n    ------\n    SdlError\n        If the request fails, the response is not valid JSON, or SDL reports a\n        non-OK status for the accession (e.g. unknown or non-public run).\n    \"\"\"\n    cached = self._cache.get(accession)\n    if cached is None:\n        payload = self._fetch(accession)\n        cached = self._cache[accession] = _parse_files(payload, accession)\n    return list(cached)\n</code></pre>"},{"location":"reference/#labdata.ncbi.config","title":"config","text":"<p>NCBI E-utilities credential resolution.</p> <p>NCBI asks every E-utilities client to identify itself with an email address, and optionally accepts an API key that raises the rate limit from 3 to 10 requests per second. This module resolves those two values from, in order:</p> <ol> <li>the <code>NCBI_EMAIL</code> / <code>NCBI_API_KEY</code> environment variables;</li> <li>a cached config file at <code>&lt;cache&gt;/ncbi.toml</code>;</li> <li>an interactive prompt (only on a TTY), whose answers are then cached.</li> </ol> <p>In a non-interactive context with nothing configured, :func:<code>load_credentials</code> raises :class:<code>CredentialsError</code> with an actionable message rather than hanging.</p>"},{"location":"reference/#labdata.ncbi.config.NcbiCredentials","title":"NcbiCredentials  <code>dataclass</code>","text":"<pre><code>NcbiCredentials(email: str, api_key: str | None = None)\n</code></pre> <p>NCBI E-utilities credentials.</p> <p>Parameters:</p> Name Type Description Default <code>email</code> <code>str</code> <p>Contact email reported to NCBI on every request (required).</p> required <code>api_key</code> <code>str or None</code> <p>Optional NCBI API key; raises the request rate limit when present.</p> <code>None</code>"},{"location":"reference/#labdata.ncbi.config.config_path","title":"config_path","text":"<pre><code>config_path() -&gt; Path\n</code></pre> <p>Return the path to the cached credentials file (which may not exist).</p> <p>Returns:</p> Type Description <code>Path</code> <p><code>&lt;cache&gt;/ncbi.toml</code>.</p> Source code in <code>src/labdata/ncbi/config.py</code> <pre><code>def config_path() -&gt; Path:\n    \"\"\"Return the path to the cached credentials file (which may not exist).\n\n    Returns\n    -------\n    pathlib.Path\n        ``&lt;cache&gt;/ncbi.toml``.\n    \"\"\"\n    return liulab_data_cache_dir() / CONFIG_FILENAME\n</code></pre>"},{"location":"reference/#labdata.ncbi.config.load_credentials","title":"load_credentials","text":"<pre><code>load_credentials(\n    *, allow_prompt: bool = True\n) -&gt; NcbiCredentials\n</code></pre> <p>Resolve NCBI credentials from env vars, the cache file, or a prompt.</p> <p>Parameters:</p> Name Type Description Default <code>allow_prompt</code> <code>bool</code> <p>When <code>True</code> (default) and nothing is configured, prompt interactively on a TTY and cache the answers. When <code>False</code>, never prompt.</p> <code>True</code> <p>Returns:</p> Type Description <code>NcbiCredentials</code> <p>The resolved credentials.</p> <p>Raises:</p> Type Description <code>CredentialsError</code> <p>If no credentials are configured and prompting is disabled or impossible (e.g. a non-interactive process).</p> Source code in <code>src/labdata/ncbi/config.py</code> <pre><code>def load_credentials(*, allow_prompt: bool = True) -&gt; NcbiCredentials:\n    \"\"\"Resolve NCBI credentials from env vars, the cache file, or a prompt.\n\n    Parameters\n    ----------\n    allow_prompt : bool\n        When ``True`` (default) and nothing is configured, prompt interactively\n        on a TTY and cache the answers. When ``False``, never prompt.\n\n    Returns\n    -------\n    NcbiCredentials\n        The resolved credentials.\n\n    Raises\n    ------\n    CredentialsError\n        If no credentials are configured and prompting is disabled or impossible\n        (e.g. a non-interactive process).\n    \"\"\"\n    creds = _from_env() or _from_file()\n    if creds is not None:\n        return creds\n\n    if allow_prompt and _is_interactive():\n        return prompt_and_save()\n\n    raise CredentialsError(\n        f\"no NCBI credentials found. Set the {EMAIL_ENV} environment variable \"\n        f\"(and optionally {API_KEY_ENV}), or run `labdata ncbi configure`.\"\n    )\n</code></pre>"},{"location":"reference/#labdata.ncbi.config.save_credentials","title":"save_credentials","text":"<pre><code>save_credentials(creds: NcbiCredentials) -&gt; Path\n</code></pre> <p>Persist credentials to the cache file with owner-only permissions.</p> <p>Parameters:</p> Name Type Description Default <code>creds</code> <code>NcbiCredentials</code> <p>The credentials to write.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>The path the credentials were written to.</p> Source code in <code>src/labdata/ncbi/config.py</code> <pre><code>def save_credentials(creds: NcbiCredentials) -&gt; Path:\n    \"\"\"Persist credentials to the cache file with owner-only permissions.\n\n    Parameters\n    ----------\n    creds : NcbiCredentials\n        The credentials to write.\n\n    Returns\n    -------\n    pathlib.Path\n        The path the credentials were written to.\n    \"\"\"\n    ensure_cache_dir()\n    path = config_path()\n    path.write_text(_to_toml(creds), encoding=\"utf-8\")\n    path.chmod(0o600)\n    return path\n</code></pre>"},{"location":"reference/#labdata.ncbi.config.prompt_and_save","title":"prompt_and_save","text":"<pre><code>prompt_and_save() -&gt; NcbiCredentials\n</code></pre> <p>Prompt for credentials interactively and cache them.</p> <p>Unlike :func:<code>load_credentials</code>, this always prompts (overwriting any cached file), which is what <code>labdata ncbi configure</code> wants.</p> <p>Returns:</p> Type Description <code>NcbiCredentials</code> <p>The entered credentials, already written to the cache file.</p> Source code in <code>src/labdata/ncbi/config.py</code> <pre><code>def prompt_and_save() -&gt; NcbiCredentials:\n    \"\"\"Prompt for credentials interactively and cache them.\n\n    Unlike :func:`load_credentials`, this always prompts (overwriting any cached\n    file), which is what ``labdata ncbi configure`` wants.\n\n    Returns\n    -------\n    NcbiCredentials\n        The entered credentials, already written to the cache file.\n    \"\"\"\n    creds = _prompt()\n    save_credentials(creds)\n    return creds\n</code></pre>"},{"location":"tutorials/geo-series/","title":"GEO Series: from study to FASTQ","text":"<p>This tutorial goes from a GEO accession to downloaded FASTQ in a few short steps, using a real study \u2014 GSE47966 (Global epigenomic reconfiguration during mammalian brain development). First, make sure you've installed the package and set your NCBI email.</p>"},{"location":"tutorials/geo-series/#1-look-up-a-geo-series","title":"1. Look up a GEO Series","text":"<p>Start from any GEO accession (<code>GSE\u2026</code>). Building the handle is instant; reading a field is what triggers the lookup.</p> <pre><code>from labdata import Series\n\ngse = Series(\"GSE47966\")     # just a handle \u2014 no network yet\n\ngse.title         # 'Global epigenomic reconfiguration during mammalian brain development'\ngse.organism      # 'Homo sapiens; Mus musculus'\ngse.platforms     # [Platform('GPL13112'), Platform('GPL11154')]\ngse.samples       # [Sample('GSM1173819'), Sample('GSM1173773'), ...]   (65 samples)\ngse.experiments   # [Experiment('SRX314994'), Experiment('SRX314993'), ...]   (65 experiments)\ngse.url           # 'https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE47966'\n</code></pre> <p>Each linked item (a sample, an experiment, a run) is its own object you can drill into the same way. See the API reference for everything they expose.</p>"},{"location":"tutorials/geo-series/#2-get-the-sra-run-table","title":"2. Get the SRA run table","text":"<p>Want the table you'd normally click through on NCBI's SRA Run Selector \u2014 one row per sequencing run, with library and instrument details? Ask for it directly. It comes back as a pandas <code>DataFrame</code>, so you can filter, sort, and export it however you like.</p> <pre><code>table = gse.make_sra_run_table()      # one row per run (392 in this study)\n\ntable[[\"Run\", \"Instrument\", \"ReadStructure\", \"Bases\", \"LibraryLayout\"]].head(3)\n</code></pre> <pre><code>         Run           Instrument ReadStructure        Bases LibraryLayout\n0  SRR907554  Illumina HiSeq 2000            50  10100000000        SINGLE\n1  SRR907555  Illumina HiSeq 2000            50  10100000000        SINGLE\n2  SRR907556  Illumina HiSeq 2000            50  10100000000        SINGLE\n...\n</code></pre> <p><code>ReadStructure</code> gives the length of each read in a spot, joined with <code>+</code>: a single-end 50\u00a0bp run reads <code>50</code>, while a paired run with a 28\u00a0bp barcode and a 94\u00a0bp cDNA read reads <code>28+94</code> \u2014 a quick way to spot single-cell chemistries.</p> <pre><code>table.to_csv(\"GSE47966_runs.csv\", index=False)   # hand it off to collaborators\n</code></pre>"},{"location":"tutorials/geo-series/#3-download-all-fastq-files","title":"3. Download all FASTQ files","text":"<p>One call fetches the FASTQ for every run in the study, using sra-tools under the hood. Files are organized by study \u2192 experiment \u2192 run and gzipped for you:</p> <pre><code>gse.download(\"./fastq\", n_parallel=4)   # 4 runs at a time\n</code></pre> <pre><code>{'SRR921999': True, 'SRR922000': True, 'SRR922001': True, ...}     # each run: success?\n</code></pre> <p>The files land in a tidy tree:</p> <pre><code>fastq/\n\u2514\u2500\u2500 GSE47966/\n    \u251c\u2500\u2500 SRX314994/\n    \u2502   \u251c\u2500\u2500 SRR921999.fastq.gz\n    \u2502   \u251c\u2500\u2500 SRR922000.fastq.gz\n    \u2502   \u2514\u2500\u2500 ...\n    \u2514\u2500\u2500 SRX314993/\n        \u2514\u2500\u2500 ...\n</code></pre> <p>Good to know</p> <ul> <li>This downloads real sequencing data, which can be large \u2014 point   <code>output_dir</code> somewhere with room to spare.</li> <li>GSE47966's runs are single-end, so each gives one <code>.fastq.gz</code>; paired-end runs   produce <code>_1</code>/<code>_2</code> files instead.</li> <li>Each finished run drops a hidden <code>.&lt;run&gt;.success</code> marker, so re-running   <code>download()</code> skips what's already done and resumes the rest.</li> <li>Pass <code>keep_sra=True</code> to keep each run's <code>.sra</code> next to its FASTQ instead of   deleting it after extraction (useful for re-extracting later).</li> <li>Needs <code>sra-tools</code> and <code>pigz</code> on your system (see Installation).</li> </ul> <p>Just one experiment?</p> <p><code>Series.download</code> does the whole study. To grab a single experiment instead, use <code>SraDownloader</code> \u2014 see the API reference.</p>"},{"location":"tutorials/geo-series/#4-grab-the-supplementary-files","title":"4. Grab the supplementary files","text":"<p>Raw FASTQ isn't the whole story. Authors usually attach supplementary files \u2014 processed matrices, peak calls, bigWigs, READMEs \u2014 to the Series. List them and grab their download URLs directly:</p> <pre><code>gse.supplementary_files       # ['GSE47966_RAW.tar', 'GSE47966_README.txt', 'filelist.txt']\ngse.supplementary_file_urls   # full https URLs you can download\ngse.supplementary_http_url    # the suppl/ directory itself\n</code></pre> <p>These are often where the useful, ready-to-analyze outputs live \u2014 see what's not in GEO for why they matter.</p>"},{"location":"tutorials/geo-series/#going-deeper","title":"Going deeper","text":"<p>This tutorial covers the common path. For the full picture \u2014 every property of <code>Series</code>, <code>Sample</code>, <code>Platform</code>, <code>Experiment</code>, <code>Run</code>, and <code>BioProject</code>, plus the downloader options \u2014 head to the API reference.</p>"},{"location":"tutorials/understanding-geo/","title":"Understanding GEO &amp; SRA","text":"<p>New to NCBI's sequencing databases? This page gives you just enough of the mental model to use <code>labdata</code> confidently. For the hands-on walkthrough, see the GEO Series tutorial.</p>"},{"location":"tutorials/understanding-geo/#two-databases-one-study","title":"Two databases, one study","text":"<p>A sequencing study usually lives in two linked NCBI databases:</p> <ul> <li>GEO \u2014 the processed, expression-focused side (count matrices, the study's   description, sample list).</li> <li>SRA \u2014 the raw reads side (the actual FASTQ files you download).</li> </ul> <p>GEO records point into SRA, so you can start from a GEO accession and follow the links all the way down to raw data.</p>"},{"location":"tutorials/understanding-geo/#the-hierarchy-study-sample-experiment-run","title":"The hierarchy: study \u2192 sample \u2192 experiment \u2192 run","text":"<p>Everything is organized as a tree, from a big project down to individual FASTQ files. Each level has its own ID prefix \u2014 and its own class in <code>labdata</code>:</p> Level Example ID What it is <code>labdata</code> class BioProject <code>PRJNA208503</code> the whole study / \"the paper\" <code>BioProject</code> GEO Series <code>GSE47966</code> the study, as seen in GEO <code>Series</code> GEO Platform <code>GPL13112</code> the sequencer used <code>Platform</code> GEO Sample <code>GSM1173819</code> one biological sample <code>Sample</code> SRA Experiment <code>SRX314994</code> one library + sequencing design <code>Experiment</code> SRA Run <code>SRR921999</code> the raw FASTQ files <code>Run</code> <p>(IDs above are from the example study GSE47966.)</p> <p>A few things worth remembering:</p> <ul> <li>It's one-to-many at every level: one project has many samples, each sample can   have several experiments, and each experiment can have several runs.</li> <li><code>SRR</code> (the run) is what you actually download \u2014 FASTQ files live at the run   level.</li> <li>You'll also see <code>SAMN\u2026</code> (BioSample) \u2014 e.g. <code>SAMN02206500</code> \u2014 the SRA-side name for   the biological material behind a <code>GSM</code>. <code>labdata</code> surfaces it as a <code>BioSample</code> field   (e.g. a column in the run table) rather than a separate class.</li> </ul>"},{"location":"tutorials/understanding-geo/#how-labdata-reaches-this-data","title":"How <code>labdata</code> reaches this data","text":"<p>The package talks to NCBI for you through two channels:</p> <ul> <li>Metadata \u2192 NCBI Entrez (E-utilities). Every <code>Series</code>, <code>Sample</code>, <code>Experiment</code>,   and <code>Run</code> fetches its details from Entrez on demand \u2014 building the object costs   nothing; the network call happens the first time you read a field, and is cached   afterward.</li> <li>Raw FASTQ \u2192 sra-tools. <code>download()</code> shells   out to <code>prefetch</code> + <code>fasterq-dump</code> to pull and extract the reads for each run.</li> </ul> <p>Because links return real objects, you can walk the tree in plain Python:</p> <pre><code>from labdata import Series\n\ngse = Series(\"GSE47966\")         # GEO Series (GSE)\n\nfor experiment in gse.experiments:   # SRX level\n    for run in experiment.runs:      # SRR level \u2014 the downloadable units\n        print(experiment.accession, run.accession)\n</code></pre> <p>That's the whole idea: start at a GEO Series, follow the links down to runs, then download. The GEO Series tutorial shows it end to end.</p>"},{"location":"tutorials/understanding-geo/#whats-not-in-geo","title":"What's not in GEO","text":"<p>The structured records above (samples, platforms, experiments, runs) are only part of a study. Two things commonly live outside that metadata \u2014 don't forget them:</p> <ul> <li> <p>Supplementary files. The actual ready-to-analyze outputs \u2014 count matrices,   peak calls, bigWig tracks, READMEs \u2014 are usually attached to the Series as   supplementary files, not encoded in the metadata. They're often the most useful   part. List and fetch them directly:</p> <pre><code>gse.supplementary_files       # ['GSE47966_RAW.tar', 'GSE47966_README.txt', 'filelist.txt']\ngse.supplementary_file_urls   # full https URLs you can download\n</code></pre> </li> <li> <p>Whatever the paper kept elsewhere. GEO doesn't capture everything. Always read   the paper's \"Data Availability\" and \"Code Availability\" statements: raw or   controlled-access data may sit in another repository (dbGaP, EGA, Zenodo, \u2026), and the   analysis code typically lives on GitHub \u2014 none of which a GEO record will point you   to.</p> </li> </ul>"}]}